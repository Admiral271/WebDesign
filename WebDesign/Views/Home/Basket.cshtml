<div class="text-center" style="margin-left: 120px;">
    <div id="home">
    <div class="row">
        <div class="col-md-4" v-for="product in products">
            <div v-if="in_basket.includes(product.id)">
                <div class="card" style="width:230px; border-radius: 10%">
                    <br />
                    <div class="align-content-center">
                        <img :src="product.photo" style="width: 200px; height: 200px; border-radius:10%;" />
                    </div>
                    {{ product.name }}
                    <br />
                    {{product.cost}}
                    <br />
                    <p style="font-size: 12px;">{{product.about}}</p>
                </div>
                <br />
            </div>
        </div>
    </div>
</div>
</div>